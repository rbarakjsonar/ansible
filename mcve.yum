- name: MCVE
  hosts: localhost
  tasks:
    - name: Install package
      debug: var=test
      debug: var=inventory_hostname
      yum: name=perl
      register: test
      async: 1200
      poll: 5
    - debug:
        var={{ test }}
    - debug:
        var={{ test.files }}
    - debug:
        msg: "I was expecting to see the 'yum' output above"
